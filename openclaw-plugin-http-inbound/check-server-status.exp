#!/usr/bin/expect -f

set timeout 60
set password "BtMZf4yGGdR0tGEj"
set host "root@154.9.252.35"

spawn ssh $host
expect "password:"
send "$password\r"

expect "# "
send "ps aux | grep 'openclaw gateway'\r"

expect "# "
send "sleep 3\r"

expect "# "
send "curl -s http://localhost:3000/health\r"

expect "# "
send "tail -30 /tmp/openclaw/openclaw-2026-03-10.log | grep -E '(startAccount|HTTP API server listening)'\r"

expect "# "
send "exit\r"

expect eof
