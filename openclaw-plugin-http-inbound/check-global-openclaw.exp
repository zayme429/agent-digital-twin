#!/usr/bin/expect -f

set timeout 60
set password "BtMZf4yGGdR0tGEj"
set host "root@154.9.252.35"

spawn ssh $host
expect "password:"
send "$password\r"

expect "# "
send "find /root/.nvm/versions/node/v23.3.0/lib/node_modules/openclaw -name '*http-inbound*' -o -name 'channel.js' | grep -i http | head -10\r"

expect "# "
send "exit\r"

expect eof
